# 🚀 Rabbit AI 前端 - 生产环境部署指南

**更新日期**: 2026-01-05  
**状态**: ✅ 生产就绪  
**版本**: v1.0

---

## 📋 部署前检查清单

### ✅ 代码质量
- [x] P0/P1 问题已全部修复
- [x] 翻译完整性检查通过
- [x] Linter 无错误
- [x] 生产环境日志配置优化

### ✅ 环境配置
- [x] `.env` 文件已配置
- [x] API Base URL 已设置
- [x] 合约地址已验证
- [x] WalletConnect Project ID 已配置

### ✅ 性能优化
- [x] 代码分割（Code Splitting）已配置
- [x] Legacy 浏览器支持（Android 5.0+）
- [x] 调试日志自动移除
- [x] 资源压缩和混淆

---

## 🔧 生产环境配置详解

### 1. 日志处理策略

#### ✅ 当前配置（已优化）

**vite.config.ts - esbuild 配置**:
```typescript
esbuild: {
  // 🟢 生产环境移除调试日志，但保留 console.error
  pure: mode === 'production' ? [
    'console.log',
    'console.info', 
    'console.debug',
    'console.warn'
  ] : [],
  drop: mode === 'production' ? ['debugger'] : [],
}
```

**vite.config.ts - legacy terser 配置**:
```typescript
terserOptions: {
  compress: {
    // 🟢 Legacy 版本也移除调试日志，保留 console.error
    pure_funcs: mode === 'production' ? [
      'console.log',
      'console.info',
      'console.debug',
      'console.warn'
    ] : [],
  },
}
```

#### 📊 日志策略说明

| 方法 | 开发环境 | 生产环境 | 说明 |
|------|---------|---------|------|
| `console.log()` | ✅ 保留 | ❌ 移除 | 调试日志 |
| `console.info()` | ✅ 保留 | ❌ 移除 | 信息日志 |
| `console.debug()` | ✅ 保留 | ❌ 移除 | 调试日志 |
| `console.warn()` | ✅ 保留 | ❌ 移除 | 警告日志 |
| `console.error()` | ✅ 保留 | ✅ **保留** | **关键错误（用于 Sentry 监控）** |

#### 🎯 优势
- ✅ **减少包体积** - 移除所有调试日志
- ✅ **保留错误监控** - console.error 保留用于 Sentry
- ✅ **防止信息泄露** - 不会暴露敏感调试信息
- ✅ **提升性能** - 减少不必要的 console 调用

---

## 🏗️ 构建生产版本

### 方法 1: 直接构建（推荐）

```bash
# 1. 安装依赖（如果还没安装）
npm install

# 2. 构建生产版本
npm run build

# 3. 预览构建结果（可选）
npm run preview
```

### 方法 2: 使用环境变量

```bash
# 设置环境为 production
set NODE_ENV=production  # Windows CMD
$env:NODE_ENV="production"  # Windows PowerShell
export NODE_ENV=production  # Linux/macOS

# 构建
npm run build
```

### 构建输出

```
dist/
├── index.html                    # 入口 HTML
├── assets/
│   ├── index-[hash].js          # 主代码（现代浏览器）
│   ├── index-legacy-[hash].js   # Legacy 代码（旧设备）
│   ├── vendor-react-[hash].js   # React 依赖
│   ├── vendor-ethers-[hash].js  # Ethers.js 依赖
│   └── vendor-walletconnect-[hash].js # WalletConnect 依赖
└── ... (其他静态资源)
```

---

## 🚀 部署到 Vercel（推荐）

### 1. 通过 Git 部署（自动部署）

```bash
# 1. 提交更改
git add .
git commit -m "Production ready - optimized console logs"
git push origin main

# 2. Vercel 会自动检测并部署
```

### 2. 环境变量配置

在 Vercel 项目设置中配置：

```bash
VITE_API_URL=https://your-backend-api.com
VITE_APP_ENV=production
```

### 3. 构建设置

- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

---

## 📊 生产环境验证

### 1. 检查 Console 日志

在浏览器开发者工具中：

```javascript
// ✅ 这些应该不会输出任何内容
console.log('test');
console.info('test');
console.debug('test');
console.warn('test');

// ✅ 这个应该正常输出（用于错误监控）
console.error('test error');
```

### 2. 检查构建产物

```bash
# 查看构建大小
ls -lh dist/assets/

# 检查是否包含 legacy 版本
ls dist/assets/*legacy*.js
```

### 3. 验证功能

- [ ] 多语言切换正常
- [ ] 钱包连接正常
- [ ] 空投领取功能正常
- [ ] VIP 等级显示正确
- [ ] 提现功能正常
- [ ] 网络错误提示为英文

---

## 🔒 安全检查

### 1. 敏感信息检查

```bash
# 确保 .env 未被提交
git status | grep ".env"  # 应该无输出

# 检查 dist 目录是否包含敏感信息
grep -r "PRIVATE_KEY\|SECRET\|PASSWORD" dist/
```

### 2. API 密钥保护

- ✅ VITE_API_URL 使用环境变量
- ✅ WalletConnect Project ID 可公开（非敏感）
- ✅ 合约地址可公开（链上地址）

---

## 📈 性能监控

### 1. Lighthouse 评分目标

| 指标 | 目标 | 说明 |
|------|------|------|
| **Performance** | ≥ 90 | 加载和交互性能 |
| **Accessibility** | ≥ 90 | 可访问性 |
| **Best Practices** | ≥ 90 | 最佳实践 |
| **SEO** | ≥ 80 | 搜索引擎优化 |

### 2. 监控工具

- **Sentry**: 错误监控（console.error 会被捕获）
- **Vercel Analytics**: 性能和流量分析
- **Google Analytics**: 用户行为分析

---

## 🐛 故障排查

### 问题 1: 构建后页面白屏

**可能原因**:
- 环境变量未配置
- API Base URL 错误

**解决方案**:
```bash
# 检查 .env 文件
cat .env

# 检查构建日志
npm run build -- --debug
```

### 问题 2: 旧设备无法加载

**可能原因**:
- Legacy 插件未正确配置

**解决方案**:
```bash
# 确保 dist 目录包含 legacy 文件
ls dist/assets/*legacy*.js

# 重新构建
npm run build
```

### 问题 3: 调试日志仍然出现

**可能原因**:
- 使用 `npm run dev` 而不是 `npm run build`

**解决方案**:
```bash
# 确保使用生产构建
npm run build
npm run preview  # 预览生产版本
```

---

## 📝 部署后检查清单

### 立即检查（部署后 5 分钟内）
- [ ] 网站可访问
- [ ] 无 404 错误
- [ ] 钱包连接正常
- [ ] API 请求成功

### 短期监控（24 小时内）
- [ ] Sentry 无严重错误
- [ ] 用户可以正常领取空投
- [ ] 提现功能正常
- [ ] 多语言显示正确

### 长期监控（持续）
- [ ] 定期检查 Sentry 错误日志
- [ ] 监控 Vercel Analytics 性能指标
- [ ] 关注用户反馈

---

## 🎯 后续优化建议

### 短期（本周）
- [ ] 添加页面加载动画
- [ ] 优化首屏加载时间
- [ ] 添加 Service Worker（PWA）

### 中期（两周内）
- [ ] 实现 Sentry 错误分组和通知
- [ ] 添加性能监控面板
- [ ] 优化图片和字体加载

### 长期（一个月内）
- [ ] 实现 A/B 测试
- [ ] 添加用户行为分析
- [ ] 优化 SEO（元标签、结构化数据）

---

## 📞 技术支持

**问题反馈**: 
- GitHub Issues: [your-repo]/issues
- Sentry Dashboard: [your-sentry-url]
- Vercel Dashboard: [your-vercel-url]

---

**✅ 生产就绪 | 🚀 可以部署！**

